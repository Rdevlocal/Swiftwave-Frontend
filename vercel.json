{
    "version": 2,
    "env": {
      "MY_VARIABLE": true
    },
    "build": {
      "env": {
        "MY_VARIABLE": true
      }
    },
    "builds": [
      {
        "src": "nuxt.config.js",
        "use": "@nuxtjs/vercel-builder",
        "config": {
          "serverFiles": ["server-middleware/**"]
        }
      }
      {
        "buildCommand": "next build"
      }
    ],
    "routes": [
      {
        "src": "/app",
        "dest": "/app/page.tsx"
      }
    ]
  }