{
    "version": 2,
    "env": {
      "MY_VARIABLE": true
    },
    "build": {
        "buildCommand": "next build"
      }
    },
    "builds": [
      {
        "src": "nuxt.config.js",
        "use": "@nuxtjs/vercel-builder",
        "config": {
          "serverFiles": ["server-middleware/**"]
        }
      }
      {
        "devCommand": "next dev"
      }
    ],
    "routes": [
      {
        "src": "/app",
        "dest": "/app/page.tsx"
      }
    ]
  }